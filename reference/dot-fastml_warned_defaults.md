# Environment for Tracking Warned Defaults

Internal environment to track which default warnings have been shown in
the current session to avoid duplicate warnings.

## Usage

``` r
.fastml_warned_defaults
```

## Format

An object of class `environment` of length 0.
