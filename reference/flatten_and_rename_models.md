# Flatten and Rename Models

Flatten and Rename Models

## Usage

``` r
flatten_and_rename_models(models)
```

## Arguments

- models:

  A named list of model objects or nested named lists of model objects.
  Nested entries are flattened and renamed using the pattern \`algorithm
  (engine)\`.

## Value

A flat named list of model objects.
