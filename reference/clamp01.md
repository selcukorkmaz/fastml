# Clamp Values to \[0, 1\]

Truncates a numeric vector so all values lie within the \[0, 1\]
interval.

## Usage

``` r
clamp01(x)
```

## Arguments

- x:

  A numeric vector.

## Value

The clamped numeric vector.
